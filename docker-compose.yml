services:
  games:
    build: .
    container_name: text-games
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      HOST: "0.0.0.0"
      PORT: "3000"
      # nginx 以 /webgame 反代时取消下一行注释，或在 .env 中设置
      # BASE_PATH: "/webgame"
    restart: unless-stopped
